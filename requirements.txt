# Pinned to the exact versions running successfully on the AI EC2 box
# (captured from `pip freeze` on 2026-07-13). Audit finding H3: unpinned deps
# made builds non-reproducible — a silent upstream release could break or
# backdoor a deploy. Every version below is one that is already known to work.
#
# NOTE (worth acting on): plain `torch` pulls the CUDA build, which drags in the
# whole nvidia stack (nvidia-cublas, cuda-toolkit, cudnn, triton — several GB) on
# a CPU-only box. To install CPU-only wheels instead, run pip with:
#     pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# That is a big win on deploy time and disk. Not baked in here because it changes
# how torch resolves, and this file's job is to reproduce the known-good set.

# ── Django + DRF (production runtime) ─────────────────────────────────────────
Django==5.2.14
djangorestframework==3.17.1
django-cors-headers==4.9.0
gunicorn==26.0.0
python-dotenv==1.2.2

# ── LLM providers / HTTP ──────────────────────────────────────────────────────
groq==1.2.0
google-genai==2.2.0
ollama==0.6.2
httpx==0.28.1
requests==2.34.1

# ── Cache / rate limiting ─────────────────────────────────────────────────────
redis==7.4.0

# ── Scientific solver (sandboxed code execution) ──────────────────────────────
numpy==1.26.4
sympy==1.14.0
scipy==1.17.1
matplotlib==3.10.9
rdkit==2026.3.1
Pint==0.25.3
PubChemPy==1.0.5

# ── Speech-to-text / media ────────────────────────────────────────────────────
faster-whisper==1.2.1
openai-whisper==20250625
moviepy==2.2.1
yt-dlp==2026.3.17
PyAudio==0.2.14

# ── OCR / vision / images ─────────────────────────────────────────────────────
easyocr==1.7.2
opencv-python==4.8.1.78
mediapipe==0.10.21
Pillow==10.4.0
pdfplumber==0.11.4
pdf2image==1.17.0

# ── NLP / ML ──────────────────────────────────────────────────────────────────
torch==2.12.0
transformers==4.41.2
tokenizers==0.19.1
huggingface-hub==0.24.7
safetensors==0.4.4
accelerate==0.33.0
spacy==3.8.14
nltk==3.9.4
rake-nltk==1.0.6
scikit-learn==1.8.0
xgboost==3.2.0
networkx==3.6.1
datasketch==1.6.5
faiss-cpu==1.8.0

# ── Legacy FastAPI runtime (retired to archive/fastapi_legacy/) ───────────────
# Kept pinned so the archived app still installs if anyone needs to run it.
# Safe to drop once you're certain nothing depends on it.
fastapi==0.129.0
uvicorn==0.41.0
python-multipart==0.0.22

# ── Formula knowledge base for the scientific solver ──────────────────────────
# Grounds the solver with formulas retrieved from the indexed PDFs in
# data/knowledge_base (IIT-JEE / NEET formula sheets), improving accuracy on
# coaching doubts. torch + transformers are already present, so this is a small
# addition. The embedding model is loaded LAZILY (first retrieval only), so it
# costs nothing at boot or in the solver's sandbox subprocess.
#
# NOTE: the KB is used for the COACHING vertical only — grounding a Class 1-10
# answer with IIT-JEE formulae would push it above grade level (see
# scientific_solver.solve). If it cannot be loaded, formula_retriever degrades
# gracefully and the solver still runs.
sentence-transformers==3.0.1
