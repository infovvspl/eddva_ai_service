"""Scientific solver library (symbolic compute + formula retrieval).

Relocated from the legacy FastAPI app/ package; imported by the Django doubt
endpoint (bridge.resolve_doubt) to solve physics/chemistry/math doubts before
falling back to the LLM.
"""
